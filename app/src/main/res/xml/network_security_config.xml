<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Default: block cleartext for unknown domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext (HTTP) for the API server until HTTPS is available -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">havostech-001-site2.atempurl.com</domain>
    </domain-config>

    <!-- Allow debug builds to trust user-installed CA certs (for Charles/mitmproxy) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
