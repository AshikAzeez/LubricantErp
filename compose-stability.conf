// Classes in these packages will be treated as stable by the Compose compiler.
// This avoids unnecessary recompositions when domain models (which are all
// immutable data classes with val-only properties) are passed to composables.
com.havos.lubricerp.feature_reports.domain.model.*
com.havos.lubricerp.core.common.*
